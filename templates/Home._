<h1>.toJSON()<small>.org</small></h1>
<p class="lead muted">a simple service to convert between different serialized data formats</p>

<form action="/upload" method="post" enctype="multipart/form-data">
  <input type="file" class="input-large" name="file" />
</form>

<div class="muted">
  Supported formats:
  <ul>
    <% _.each(formats, function(format) { %>
      <li><small><%= format %></small></li>
    <% }); %>
  </ul>
</div>
